import {createTheme} from '@mui/material';

const theme = createTheme({
  spacing: 8,
  typography: {
    fontSize: 16,
  },
  palette: {
    primary: {
      main: '#f7f7f7',
      secondary: '#000',
    },
  }
});

export default theme;
